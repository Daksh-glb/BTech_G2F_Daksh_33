#include <stdio.h>

int main() {
    int age;

    printf("Enter age: ");
    scanf("%d", &age);

    if (age >= 18)
        printf("Eligible for voting");
    else if (age >= 0)
        printf("Not eligible for voting");
    else
        printf("Invalid age");

    return 0;
}
